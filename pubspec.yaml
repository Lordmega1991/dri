name: Ola_app
description: Gerenciador de Defesas do DRI
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.10.2
  intl: ^0.20.2
  pdf: ^3.10.4
  printing: ^5.9.2
  path_provider: ^2.0.0
  permission_handler: ^12.0.1
  open_file: ^3.2.0
  path: ^1.8.0
  archive: ^4.0.7
  file_picker: ^10.3.10
  http: ^1.2.2
  googleapis: ^16.0.0
  google_sign_in: ^7.2.0
  googleapis_auth: ^2.0.0
  url_launcher: ^6.1.11
  flutter_hooks: ^0.21.3+1
  flutter_staggered_grid_view: ^0.7.0
  file_saver: ^0.3.1
  share_plus: ^12.0.1
  dart_jsonwebtoken: ^3.3.1
  flutter_downloader: ^1.12.0
  screenshot: ^3.0.0
  image_gallery_saver: ^2.0.2

  
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # ADICIONE ESTA PARTE PARA OS ASSETS:
  assets:
    - assets/ufpb.png
    - assets/service_account.json